@model IEnumerable<CareeriaDevsApp.Opiskelija>

@{
    ViewBag.Title = "Index";
}

<h2>Opiskelijat</h2>

<p>
    @Html.ActionLink("Luo uusi", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Etunimi
        </th>
        <th>
            Sukunimi
        </th>
        <th>
            Asuinpaikka
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.etunimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sukunimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Postitoimipaikka.postitoimipaikka1)
        </td>
        <td>
            @Html.ActionLink("Muokkaa", "Edit", new { id=item.opiskelija_Id }) |
            @*@@Html.ActionLink("Details", "Details", new { id=item.opiskelija_Id }) | *@
            @Html.ActionLink("Poista", "Delete", new { id=item.opiskelija_Id })
        </td>
    </tr>
}

</table>
