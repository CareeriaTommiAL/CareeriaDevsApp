@model IEnumerable<CareeriaDevsApp.Yritys>

@{
    ViewBag.Title = "Index";
}

<h2>Yritykset</h2>

<p>
    @Html.ActionLink("Luo uusi", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Yrityksen nimi
        </th>
        <th>
            Y-tunnus
        </th>
        <th>
            Lähiosoite
        </th>
        <th>
            Paikkakunta
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.yrityksenNimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Y_tunnus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lahiosoite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Postitoimipaikka.postitoimipaikka1)
        </td>
        <td>
            @Html.ActionLink("Muokkaa", "Edit", new { id=item.yritys_Id }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.yritys_Id }) | *@
            @Html.ActionLink("Poista", "Delete", new { id=item.yritys_Id })
        </td>
    </tr>
}

</table>
